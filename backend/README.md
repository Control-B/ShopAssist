# Backend

Reserved for future backend services that should not live inside the embedded Next.js app.

For Phase 1, all implemented server behavior remains inside `frontend` because Shopify auth, embedded UI, and route handlers are part of the app surface.
