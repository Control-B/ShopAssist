import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function StoreDataPage() {
  return (
    <SectionPlaceholder
      title="Store Data Sharing"
      description="This section will manage what catalog and inventory context Omniweb AI is allowed to sync to the external backend."
    />
  );
}
